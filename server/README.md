# Hexagon — Server

Backend chatbota (FastAPI + LangGraph) — do zaimplementowania.

Planowana struktura:

```
server/
├── app/
│   ├── main.py          # FastAPI entrypoint
│   └── graph/           # LangGraph agent
├── requirements.txt
└── README.md
```

Frontend (`client/`) będzie komunikował się z API przez endpointy HTTP.
